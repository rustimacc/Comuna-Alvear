<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="a111rosario.comunaalvear">

    <uses-permission android:name="android.permission.CAMERA"/>//pedir permisos para usar la cámara
    <uses-permission android:name="android.permission.INTERNET" /> //Permisos para usar internet

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Medidor de Consumo"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Formulario"></activity>
        <activity android:name=".LeerCodigodeBarras"></activity>
    </application>

</manifest>